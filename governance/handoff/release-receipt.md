# 正式发布回执

记录 ID：

生成角色编号：4

生成角色名称：超级发布

生成时间（UTC）：

发布编号：

版本：

已批准 Candidate SHA：

实际部署 SHA：

发布标签：

发布时间：

目标 Worker：已核对 / 不匹配

D1：已核对 / 不匹配

MEDIA_KV：已核对 / 不匹配

Secrets/变量：已核对 / 不匹配

发布前基线：

生产预检：

Migration 执行结果：

正式部署结果：

发布后冒烟验证：

原网址核对：

前台：

后台：

图片：

视频：

二维码/访问控制：

手机端：

回滚结果（如适用）：

最终状态：PRODUCTION_VERIFIED / ROLLED_BACK / BLOCKED

下一角色：结束 / 3（超级工作）/ 4（超级发布）
